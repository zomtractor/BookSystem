import com.nhbkeshe.booksystem.pojo.Book;
import com.nhbkeshe.booksystem.util.Sqls;
import org.junit.Test;

public class AddTestBook {
/*    @Test
    public void addTestBook(){
        for(int i=0;i<6;i++){
            Book book=new Book().setBookName("testaBook"+i).setAuthorName("authorA").setPress("publisherA").setPrice(i+1)
                    .setDemo("this is A a book in order to test something");
            Sqls.bookMapper.insertBook(book);
            book=new Book().setBookName("testCbBook"+i).setAuthorName("authorC").setPress("publisherC").setPrice(i+1)
                    .setDemo("this is a C book in order to test something");
            Sqls.bookMapper.insertBook(book);
            book=new Book().setBookName("testXbBook"+i).setAuthorName("authorX").setPress("publisherX").setPrice(i+1)
                    .setDemo("this is a C book in order to test something");
            Sqls.bookMapper.insertBook(book);
        }
        Sqls.sqlSession.commit();
    }*/
}
